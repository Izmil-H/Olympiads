#include<bits/stdc++.h>
using namespace std;

int main() {

}
    /*
    int s, m, w;
    cin >> s >> m >> w;
    long long stat[s] = {0};
    int arr[w][3];
    for (int i = 0; i < w; ++i) {
        cin >> arr[i][0] >> arr[i][1] >> arr[i][2]; 
        for (int j = arr[i][0]; i <= arr[i][1]; ++i) {
            stat[j] += arr[i][2];
        }
    }

    int count = 0;
    for (int i = 0; i < w; ++i) {
        if (stat[i] < m) count++;
    }
    cout << count;
    */